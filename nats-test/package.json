{
  "name": "nats-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "publisher": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/publisher.ts",
    "listener": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/listener.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "node-nats-streaming": "^0.3.2"
  }
}
